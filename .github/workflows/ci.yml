on: [push]

name: toml test

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Edit Toml
        uses: ciiiii/toml-editor@1.0.0
        with:
          file: "Cargo.toml"
          key: "version"
          value: "v2.0.0"
      - name:Validate
        run: |
          cat Cargo.toml
     